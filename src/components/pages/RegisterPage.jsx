import Register from '../auth/Register';

const RegisterPage = () => {
    return (
        <div className="page-container">
            <Register />
        </div>
    );
};

export default RegisterPage;